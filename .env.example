PORT=4000

# Signs/verifies JWTs in src/middleware/guard.js — required for guard to
# work at all (it throws a 500 if unset, on purpose, rather than silently
# accepting unsigned tokens).
JWT_SECRET=

# Optional separate key for HMAC-hashing OTP values at rest. If omitted,
# JWT_SECRET is used. Set a different long random value in production.
OTP_SECRET=

# Sends the registration OTP via Resend's HTTPS API. Set both in production
# or POST /api/auth/register will reject at the OTP-send step instead of
# pretending an email was sent; in non-production, if either is missing the
# OTP is logged to the console instead (see auth.controller.js's
# issueRegistrationOtp).
#
# NOT raw SMTP — Render's network was found to block outbound SMTP entirely
# (port 465 and 587 both time out at the network level, regardless of
# credentials), so email must go over HTTPS instead.
RESEND_API_KEY=
OTP_FROM_EMAIL=WorkBridge <onboarding@resend.dev>

# Not yet consumed — src/db/client.js is a stub until this exists.
# Matches the local Postgres from ../docker-compose.yml once it's running:
# postgresql://<POSTGRES_USER>:<POSTGRES_PASSWORD>@localhost:5432/workbridge_db
DATABASE_URL=
